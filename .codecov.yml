# Codecov.io configuration
codecov:
  branch: main  # override the default branch

coverage:
  precision: 2  # 2 = xx.xx%, 0 = xx%
  round: nearest # default down|up|nearest
  range: 70...90 # default 70...90. red...green
